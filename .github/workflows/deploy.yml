name: Deploy to Render

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Render deploy tetikle
        run: |
          curl -s -X POST \
            -H "Authorization: Bearer rnd_WZOvZc3xYnRWxmvN80wQWQwYinmM" \
            -H "Content-Type: application/json" \
            -d '{"clearCache":"do_not_clear"}' \
            https://api.render.com/v1/services/srv-d868jcp9rddc73f34bfg/deploys
          echo "Deploy tetiklendi ✅"
